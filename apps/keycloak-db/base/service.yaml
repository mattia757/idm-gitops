apiVersion: v1
kind: Service
metadata:
  name: keycloak-db
  labels:
    app.kubernetes.io/name: keycloak-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: keycloak-db
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
