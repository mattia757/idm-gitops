apiVersion: v1
kind: Service
metadata:
  name: idmccnobackend
  labels:
    app.kubernetes.io/name: idmccnobackend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: idmccnobackend
  ports:
    - name: http
      port: 8080
      targetPort: http
